// import { Car } from "@/types";

// export default async function fetchTestCars() {
//    try {
//       const res = await fetch("/cars");

//       const cars: Car[] = await res.json();

//       return cars;
//    } catch (err) {
//       if (err instanceof Error) console.log(err.message);
//       return [];
//    }
// }
